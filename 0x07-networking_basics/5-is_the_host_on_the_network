#!/usr/bin/env bash
# This is a script that pings an IP address passed as an argument.

IP="${1}"

# filename variable
p=$(basename "${BASH_SOURCE[@]}")

if ! [ "$IP" ]; then
	echo "Usage: ${p} {IP_ADDRESS}"
else
	ping -c 5 "${IP}"
fi
